fn main(){
   let x: i32 = -42;
   let y: u64 = 100;
   println!("Signed Integer: {}", x);
   println!("Unsigned Integer {}", y);


//difference i32 can take (32 bits) and i64 (64 bits)
//range 

//floats (f32,f64) floating point types

    let pi: f64 = 3.14;
    println!("Value of pi: {}", pi);

// boolean values: true, false
    let is_snowing: bool = true;
    println!("Is it snowing ? {}", is_snowing);

// character Type - char //can only be a 
//single unicode value
    let letter: char ='x';
    println!("What do you say? {}", letter);
}